# Pour que Kibana écoute sur toutes les interfaces
server.host: "0.0.0.0"

# Utilise la substitution d'environnement pour les informations d'Elasticsearch
elasticsearch.hosts: ["${ELASTICSEARCH_URL}"]
elasticsearch.username: "${ELASTIC_USER}"
elasticsearch.password: "${ELASTIC_PASSWORD}"